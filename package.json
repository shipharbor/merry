{
  "name": "merry",
  "version": "2.0.0",
  "description": "Modular http framework",
  "main": "index.js",
  "bin": {
    "merry-pretty": "./pretty.js"
  },
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev --entry error.js --entry json.js --entry listen.js --entry log.js --entry pretty.js --entry string.js",
    "start": "node example",
    "test": "standard && npm run deps",
    "test:cov": "standard && npm run deps"
  },
  "repository": "yoshuawuyts/merry",
  "keywords": [
    "http",
    "framework",
    "modular",
    "http-framework"
  ],
  "license": "MIT",
  "dependencies": {
    "bole": "^3.0.1",
    "explain-error": "^1.0.3",
    "from2-string": "^1.1.0",
    "garnish": "^5.2.0",
    "pump": "^1.0.1",
    "server-router": "^4.0.0",
    "server-sink": "^1.0.0",
    "server-summary": "^6.0.1",
    "xtend": "^4.0.1"
  },
  "devDependencies": {
    "dependency-check": "^2.5.1",
    "istanbul": "^0.4.2",
    "standard": "^8.0.0",
    "tape": "^4.6.2"
  }
}
